cmake_minimum_required(VERSION 3.16.0)
set(EXTRA_COMPONENT_DIRS src)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(Code)
if(COMMAND littlefs_create_partition_image)
	littlefs_create_partition_image(littlefs data FLASH_IN_PROJECT)
else()
	message(STATUS "littlefs_create_partition_image not available; skipping partition image generation")
endif()
